apiVersion: batch/v1
kind: Job
metadata:
  name: doltdb-operator-test-runner-job
spec:
  parallelism: 1
  activeDeadlineSeconds: 1600
  ttlSecondsAfterFinished: 100
  template:
    spec:
      automountServiceAccountToken: true
      serviceAccountName: doltdb-operator-test-runner-sa
      containers:
        - name: doltdb-operator-test-runner
          image: localhost:5000/doltdb-operator-test-runner
          resources:
            limits:
              memory: 2Gi
            requests:
              memory: 128Mi
              cpu: 100m
      restartPolicy: Never
