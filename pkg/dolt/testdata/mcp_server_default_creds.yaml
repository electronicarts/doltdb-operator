test-cluster-0.yaml:
  behavior:
    auto_gc_behavior:
      enable: false
  log_level: trace
  listener:
    host: 0.0.0.0
    max_connections: 128
    port: 3306
  mcp_server:
    port: 7007
    user: ${DOLT_USERNAME}
    password: ${DOLT_MCP_PASSWORD}
