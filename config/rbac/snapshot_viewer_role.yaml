# permissions for end users to view snapshots.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: dolt-operator
    app.kubernetes.io/managed-by: kustomize
  name: Snapshot-viewer-role
rules:
- apiGroups:
  - REDACTED
  resources:
  - snapshots
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - REDACTED
  resources:
  - snapshots/status
  verbs:
  - get
