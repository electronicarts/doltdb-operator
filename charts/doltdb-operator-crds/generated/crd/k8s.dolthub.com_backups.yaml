---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: backups.k8s.dolthub.com
spec:
  group: k8s.dolthub.com
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.doltDBRef.name
      name: DoltDB
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup.
            properties:
              backoffLimit:
                default: 2
                description: BackoffLimit specifies the number of retries before marking
                  the backup as failed.
                format: int32
                type: integer
              databases:
                description: Databases is the list of databases to back up. If empty,
                  all databases are backed up.
                items:
                  maxLength: 80
                  type: string
                maxItems: 100
                type: array
              doltDBRef:
                description: DoltDBRef is a reference to the DoltDB cluster to back
                  up.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              imagePullSecrets:
                description: ImagePullSecrets specifies the secrets to use for pulling
                  container images.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              resources:
                description: Resources defines the compute resources for the backup
                  job.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: Storage defines where the backup will be stored.
                maxProperties: 1
                minProperties: 1
                properties:
                  doltHub:
                    description: DoltHub defines a DoltHub remote storage backend
                      for backups.
                    properties:
                      credentialsSecretKeyRef:
                        description: CredentialsSecretKeyRef is a reference to a secret
                          containing DoltHub credentials.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      remoteUrl:
                        description: RemoteURL is the DoltHub remote API URL.
                        maxLength: 512
                        type: string
                    required:
                    - remoteUrl
                    type: object
                  local:
                    description: Local defines a local filesystem storage backend
                      for backups.
                    properties:
                      path:
                        description: Path is the filesystem path for the backup.
                        maxLength: 255
                        pattern: ^/[a-zA-Z0-9_./-]+$
                        type: string
                    required:
                    - path
                    type: object
                  s3:
                    description: S3 defines an S3-compatible storage backend for backups.
                    properties:
                      accessKeyIdSecretKeyRef:
                        description: AccessKeyIdSecretKeyRef is a reference to a secret
                          containing the AWS access key ID.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                      bucket:
                        description: Bucket is the S3 bucket name.
                        maxLength: 63
                        pattern: ^[a-z0-9][a-z0-9.-]+[a-z0-9]$
                        type: string
                      endpoint:
                        description: Endpoint is a custom endpoint for S3-compatible
                          services (e.g. MinIO).
                        type: string
                      forcePathStyle:
                        description: ForcePathStyle enables path-style addressing
                          for S3-compatible services.
                        type: boolean
                      prefix:
                        description: Prefix is the path prefix within the bucket.
                        maxLength: 512
                        type: string
                      region:
                        description: Region is the AWS region for the bucket.
                        type: string
                      secretAccessKeySecretKeyRef:
                        description: SecretAccessKeySecretKeyRef is a reference to
                          a secret containing the AWS secret access key.
                        properties:
                          key:
                            type: string
                          name:
                            type: string
                        required:
                        - key
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - bucket
                    type: object
                type: object
            required:
            - doltDBRef
            - storage
            type: object
            x-kubernetes-validations:
            - message: backup spec is immutable
              rule: self == oldSelf
            - message: bad name
              rule: '!has(self.databases) || self.databases.all(d, d.matches(''^[a-zA-Z0-9_]+$''))'
            - message: name too long
              rule: '!has(self.databases) || self.databases.all(d, size(d) <= 80)'
          status:
            description: BackupStatus defines the observed state of Backup.
            properties:
              completedAt:
                description: CompletedAt is the time when the backup completed.
                format: date-time
                type: string
              conditions:
                description: Conditions for the Backup object.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              error:
                description: Error contains the error message if the backup failed.
                type: string
              phase:
                description: Phase is the current phase of the backup.
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              retryCount:
                description: RetryCount tracks the number of failed reconciliation
                  attempts.
                format: int32
                type: integer
              startedAt:
                description: StartedAt is the time when the backup started.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
