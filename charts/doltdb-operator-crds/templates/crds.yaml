{{- range $path, $_ := .Files.Glob "generated/crd/*" }}
{{- $crd := $.Files.Get $path | fromYaml }}
{{- $_ := set $crd.metadata "labels" (dict "app.kubernetes.io/name" "doltdb-operator-crds") }}
{{- if $.Values.keepCrds }}
{{- $_ := set $crd.metadata "annotations" (dict "helm.sh/resource-policy" "keep") }}
{{- end }}
{{ $crd | toYaml }}
---
{{- end }}
